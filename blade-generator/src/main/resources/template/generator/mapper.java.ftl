package ${mapperPath};

import ${entityPath}.${entityName};

/**
 * <p>
 * ${remark} Mapper 接口
 * </p>
 *
 * @author ${author}
 * @since ${createDate}
 */
public interface ${mapperName} extends BaseMapper<${entityName}> {

}